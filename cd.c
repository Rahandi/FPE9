#include"types.h"
#include"stat.h"
#include"user.h"
#include"fcntl.h"
#include"fs.h"

int main (int argc, char *argv[])
{
	if (argc < 2 || argc > 2)
	{
		printf (1, "format penggunaan: cd [direktori yang ingin dituju]\n");
		exit();
	}
}
